# Bearer token clients must supply to authenticate with this MCP server.
# Used by n8n (MCP Tool node), Claude Code, and any other MCP clients.
AUTH_TOKEN=

# n8n API key — generate in n8n Settings → API → Create API Key
N8N_API_KEY=
