# Superuser password for the shared PostgreSQL instance.
# Each service connects using this user and its own database name.
POSTGRES_PASSWORD=

# Connection string for postgres_exporter — auto-derived from POSTGRES_PASSWORD.
DATA_SOURCE_NAME=
