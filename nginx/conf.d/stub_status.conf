# Internal nginx status endpoint for Prometheus scraping.
# Port 8080 is NOT mapped to the host — accessible within Docker networks only.
server {
    listen 8080;
    server_name localhost;

    location /stub_status {
        stub_status;
        allow 172.16.0.0/12;
        deny all;
    }
}
